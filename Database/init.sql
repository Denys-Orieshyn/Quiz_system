-- ============================================================
-- Quiz System — init.sql
-- ============================================================
-- Цей файл виконується PostgreSQL при першому запуску Docker.
-- SQLAlchemy автоматично створює таблиці через models.py,
-- тому тут лише корисні початкові налаштування.
-- ============================================================

-- Встановлюємо часовий пояс
SET timezone = 'Europe/Kiev';

-- Розширення для генерації UUID (опційно, на майбутнє)
CREATE EXTENSION IF NOT EXISTS "pgcrypto";